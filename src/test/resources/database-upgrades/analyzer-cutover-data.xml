<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.8.xsd">
    <changeSet id="fixture-analyzer-cutover-data"
        author="openelis">
        <comment>Existing data before lifecycle normalization and destructive
            cleanup.</comment>
        <sql>
            INSERT INTO analyzer_profile_binding (id, profile_id, profile_revision,
            profile_fingerprint)
            VALUES (900003, 'upgrade-fixture', 1, 'sha256:' || repeat('a', 64));
            INSERT INTO analyzer_site_binding (id, profile_binding_id, created_by)
            VALUES (900003, 900003, '1');
            INSERT INTO analyzer_site_binding_revision
            (id, site_binding_id, revision_number, binding_fingerprint, created_by)
            VALUES (900003, 900003, 1, 'sha256:' || repeat('b', 64), '1');
            INSERT INTO analyzer_site_binding_test
            (site_binding_revision_id, source_row_key, mapping_state, test_id)
            VALUES (900003, 'upgrade-test', 'BOUND', (SELECT MIN(id) FROM test));
            INSERT INTO analyzer (id, name, is_active, status, analyzer_type)
            VALUES (900001, 'Empty draft', TRUE, 'PENDING_REGISTRATION',
            'HEMATOLOGY');
            INSERT INTO analyzer (id, name, is_active, status)
            VALUES (900002, 'Excluded fixture', FALSE, 'INACTIVE');
            INSERT INTO analyzer
            (id, name, is_active, status, bridge_connection_id,
            site_binding_revision_id, machine_id)
            VALUES (900003, 'Migrated fixture', TRUE, 'ACTIVE',
            'bridge-upgrade-fixture', 900003, 'legacy-fixture');
            INSERT INTO analyzer_test_map (analyzer_id, analyzer_test_name, test_id)
            VALUES (900003, 'upgrade-test', (SELECT MIN(id) FROM test));
        </sql>
    </changeSet>
</databaseChangeLog>
