package org.openelisglobal.labelpreset.valueholder;

/**
 * Source of a label preset content field. v2 only supports system-provided
 * fields; the column exists for v3+ extension (CUSTOM_FREETEXT, CUSTOM_FIXED,
 * QUERY_DRIVEN).
 */
public enum FieldSourceType {
    SYSTEM
}
