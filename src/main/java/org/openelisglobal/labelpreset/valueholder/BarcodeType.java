package org.openelisglobal.labelpreset.valueholder;

/**
 * Barcode symbology for a label preset. Persisted as STRING (see
 * label_preset.barcode_type).
 */
public enum BarcodeType {
    CODE_128, QR, DATAMATRIX
}
