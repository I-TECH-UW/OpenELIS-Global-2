<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
  xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

  <changeSet id="3.5.0-070-add-freezer-deleted-column" author="cold-storage-monitoring-audit">
    <preConditions onFail="MARK_RAN">
      <not>
        <columnExists tableName="freezer" columnName="deleted" schemaName="clinlims"/>
      </not>
    </preConditions>
    <comment>Deleting a device previously reused the "active" enable/disable flag, so
      deleted devices stayed in management lists and could be re-activated via the
      power toggle (issue #3743). A dedicated deleted flag separates "removed" from
      "temporarily disabled" without losing freezer_reading history.</comment>
    <addColumn tableName="freezer" schemaName="clinlims">
      <column name="deleted" type="BOOLEAN" defaultValueBoolean="false">
        <constraints nullable="false"/>
      </column>
    </addColumn>
  </changeSet>

</databaseChangeLog>
