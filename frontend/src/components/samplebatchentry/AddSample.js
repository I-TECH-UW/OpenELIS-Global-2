import React, { useState, useContext, useEffect } from "react";
import {
  Button,
  Form,
  Heading,
  Grid,
  Column,
  Section,
  Breadcrumb,
  BreadcrumbItem,
} from "@carbon/react";

function AddSample() {
  return (
    <div>
      <Form>
        <h3>i am tired but will still keep pushing</h3>
      </Form>
    </div>
  );
}
export default AddSample;
