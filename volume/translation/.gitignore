# A deployment's own translations are specific to that deployment and must not be
# committed into the core repository — core Transifex owns the bundled bundles
# under frontend/src/languages, and a distro's Transifex owns whatever it mounts
# here. Ignore the locale files, keep the documentation and the example.
*.json
!example.en.json
